# Vercel Environment Variables
# Add these in Vercel Dashboard -> Settings -> Environment Variables

# API URL (optional - defaults to Render URL if not set)
VITE_API_URL=https://toki-backend-78ds.onrender.com/api

# Socket URL (optional - defaults to Render URL if not set)
VITE_SOCKET_URL=https://toki-backend-78ds.onrender.com
